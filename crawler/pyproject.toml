[project]
name = "crawler"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "crawlee[all]>=0.6.11",
]

[dependency-groups]
dev = [
    "ruff>=0.12.4",
]
